# Application Configuration
APP_NAME="Proxy Checker API"
APP_VERSION="1.0.0"
APP_DESCRIPTION="FastAPI application for testing HTTP/HTTPS/SOCKS proxies"

# Server Configuration
HOST="0.0.0.0"
PORT=8000

# Proxy Testing Configuration
DEFAULT_TIMEOUT=10
DEFAULT_MAX_CONCURRENT=10
TEST_URL="http://ip-api.com/json/"

# CORS Configuration
CORS_ORIGINS=["*"]
CORS_ALLOW_CREDENTIALS=true
CORS_ALLOW_METHODS=["*"]
CORS_ALLOW_HEADERS=["*"]
